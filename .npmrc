# CI (HackOnVibe npm ci) + local installs: Solana wallet adapter pulls optional
# react-native peers that conflict with React 19.0.x. Prefer legacy peer resolution
# so lockfile installs stay consistent without forcing React upgrades mid-hackathon.
legacy-peer-deps=true
